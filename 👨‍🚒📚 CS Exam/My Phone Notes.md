
Radix -4 - base 4
Live variable
D flip flop
More and Ethernet IEEE standard
TCP - RTT , rate, amdaximum life segment to max sequence number
Persistant and non persistant http 

```
cd ~/storage/documents/My_md_Notes
git status
git pull origin main
git add .
git commit -m "update notes"
git push origin main
```